// import { createRpcClient } from "./RpcClient";

// const rpc = createRpcClient();

// // demander quelque chose à l’extension
// rpc.ask("getTime").then((time) => {
//   console.log("Heure reçue de l’extension:", time);
// });

// // notifier sans attendre de réponse
// rpc.notify("logMessage", "Hello depuis la webview!");

// // réagir à une notification venant de l’extension
// rpc.on("ping", (data) => {
//   console.log("Ping reçu:", data);
// });
